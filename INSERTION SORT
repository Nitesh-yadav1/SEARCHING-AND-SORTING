def insertionsort(arr):
    length=len(arr)
    for i in range(1,length):
        j=i-1
        temp=arr[i]
        while j>=0 and arr[j]>temp:
            arr[j+1]=arr[j]
            j=j-1
        arr[j+1]=temp

arr=[9,8,5,6,7,1]
insertionsort(arr)
print(arr)
